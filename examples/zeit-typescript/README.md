# Zeit Now with TypeScript

### Try It

```
yarn && yarn deploy && yarn dev
```

Note: `now dev` under `yarn dev` requires the project to be "linked" to remote project, hence you have to run `yarn deploy` (`now`) first.
